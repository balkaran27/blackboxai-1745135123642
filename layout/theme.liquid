<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>{{ page_title }} - IMPRESSA</title>
  <!-- Tailwind CSS CDN -->
  <script src="https://cdn.tailwindcss.com"></script>
  <!-- Google Fonts placeholders for PARODE, MOURGEN, LOVERA -->
  <link href="https://fonts.googleapis.com/css2?family=Playfair+Display&display=swap" rel="stylesheet" />
  <!-- Font Awesome CDN -->
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css" />
  <style>
    /* Custom font families placeholders */
    body {
      font-family: 'Playfair Display', serif;
      background-color: #F8F4F0; /* PORCELAIN WHITE */
      color: #1C1C1C; /* DEEP ONYX */
    }
    .warm-taupe {
      color: #B19F91;
    }
    .smoked-charcoal {
      color: #48494B;
    }
    .deep-onyx {
      color: #1C1C1C;
    }
    .bg-warm-taupe {
      background-color: #B19F91;
    }
    .bg-smoked-charcoal {
      background-color: #48494B;
    }
    .bg-deep-onyx {
      background-color: #1C1C1C;
    }
  </style>
  {{ content_for_header }}
</head>
<body class="min-h-screen flex flex-col">
  <header class="bg-porcelain-white shadow-md p-4 flex justify-between items-center">
    <div class="text-3xl font-bold warm-taupe">
      IMPRESSA
    </div>
    <nav>
      <ul class="flex space-x-6 smoked-charcoal font-semibold">
        <li><a href="/" class="hover:text-warm-taupe transition">Home</a></li>
        <li><a href="/collections/all" class="hover:text-warm-taupe transition">Shop</a></li>
        <li><a href="/cart" class="hover:text-warm-taupe transition">Cart</a></li>
      </ul>
    </nav>
  </header>

  <main class="flex-grow">
    {{ content_for_layout }}
  </main>

  <footer class="bg-deep-onyx text-porcelain-white p-6 text-center">
    &copy; {{ 'now' | date: '%Y' }} IMPRESSA. All rights reserved.
  </footer>

  {{ content_for_footer }}
</body>
</html>
